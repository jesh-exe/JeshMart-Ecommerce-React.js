1. To use routing in react
    -- npm install react-router-dom

2. To apply routing
    -- in index.js file - Wrap <App /> with <BrowserRouter></BrowserRouter>
    -- so that routing is managed by DOM

3. In our App.js file, make <Routes></Routes> which has <Route></Route> inside it
    Route contains all the information for routing to specific uri


